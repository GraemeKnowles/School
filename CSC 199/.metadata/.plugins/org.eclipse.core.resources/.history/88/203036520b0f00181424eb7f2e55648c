package recursiveDescent;

abstract class Token{
	// Returns the infix notation of the token
	public abstract String toInfix();
	// Returns if the input character c, is one of the possible first characters of the token
	public abstract boolean isFirstCharValid(char c);
}
